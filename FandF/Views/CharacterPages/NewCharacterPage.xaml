<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
	x:Class="FandF.NewCharacterPage" Title="{Binding Title}">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Spacing="20" Padding="15">
                <Label Text="Name" FontSize="Medium" />
                <Entry Text="{Binding Character.Name}" FontSize="Small" />
                <Label Text="Image URI" FontSize="Medium" />
                <Editor Text="{Binding Character.Image}" FontSize="Small" Margin="0" />
                <Label Text="Strength" FontSize="Medium" />
                <Editor Text="{Binding Character.Str}" FontSize="Small" />
                <Label Text="Defense" FontSize="Medium" />
                <Editor Text="{Binding Character.Def}" FontSize="Small" />
                <Label Text="Dexterity" FontSize="Medium" />
                <Editor Text="{Binding Character.Dex}" FontSize="Small" />
                <Label Text="Health" FontSize="Medium" />
                <Editor Text="{Binding Character.Health}" FontSize="Small" />
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>
